// DemoLoginModal has been removed as part of the migration to Supabase Auth.
// The application now uses real authentication with Google OAuth and email/password.

export const DemoLoginModal = null;
export default DemoLoginModal;
